# 会话 06：实例初始化与用户密码模型

## 我完成了什么
- 用户可见结果：
- 从入口到结果的调用链：
- 修正application-dev.yml on-profile: local -> dev

## 我理解的核心设计
- 关键不变量：
- 事务/一致性边界：
- 权限与安全边界：
- 为什么没有选择另一种方案：

## 失败路径
- 触发方式：
- 系统如何失败：
- 数据是否保持正确：
- 如何定位与恢复：

## 测试证据
- 测试名与断言：
- 它不能证明什么：

## 仍不清楚的问题
- ...

## 3 个复盘问题
1. 为什么必须锁定 instance_settings 单例行，而不能先查询 users 是否为空？

2. 为什么唯一约束仍然不能替代应用层的初始化检查和稳定 409 错误？

3. BCrypt 为什么每次生成不同 Hash 仍能验证同一个密码，以及 72-byte 上限为什么必须在哈希前检查？
